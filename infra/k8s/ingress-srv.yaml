apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-srv
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: sebsonic2o.blog.com
      http:
        paths:
          - path: /posts
            pathType: Prefix
            backend:
              service:
                name: posts-clusterip-srv
                port:
                  number: 4000
